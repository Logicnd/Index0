// Generated by scripts/generate-client-config.js — edit .env / Vercel env vars instead
window.INDEX0_CONFIG = {
  "apiBase": "http://localhost:3000",
  "backendUrl": "https://index0-backend.onrender.com"
};

(function resolveApiBase() {
  const cfg = window.INDEX0_CONFIG || {};
  const configured =
    typeof cfg.apiBase === 'string' ? cfg.apiBase.trim() : null;
  const isLocal =
    window.location.hostname === 'localhost' ||
    window.location.hostname === '127.0.0.1';

  if (configured) {
    window.INDEX0_API_BASE = configured.replace(/\/$/, '');
  } else if (isLocal) {
    window.INDEX0_API_BASE = 'http://localhost:3000';
  } else {
    window.INDEX0_API_BASE = '';
  }
})();
